Reinhardt Cloud Operator has been installed.

Platform: {{ .Values.platform }}

Enabled features:
  - Database: {{ .Values.features.database }}
  - Cache: {{ .Values.features.cache }}
  - Ingress: {{ .Values.features.ingress }}
  - Autoscaling: {{ .Values.features.autoscaling }}
  - Storage: {{ .Values.features.storage }}
  - Worker: {{ .Values.features.worker }}

To verify the installation:

  kubectl get pods -n {{ .Values.namespace }}
  kubectl get deployment -n {{ .Values.namespace }}

To deploy a sample application:

  cat <<EOF | kubectl apply -f -
  apiVersion: paas.reinhardt-cloud.dev/v1alpha2
  kind: ReinhardtApp
  metadata:
    name: sample-app
    namespace: default
  spec:
    image: nginx:latest
    replicas: 1
  EOF

To check the operator logs:

  kubectl logs -n {{ .Values.namespace }} deployment/{{ include "reinhardt-cloud-operator.fullname" . }}
