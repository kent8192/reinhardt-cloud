# Ignore untracked TOML files unless explicitly allowed below.
*.toml

# Tracked settings files MUST contain no literal secrets. Secrets are injected
# at runtime via REINHARDT_* environment variables.
!base.toml
!local.toml
!ci.toml
!staging.toml
!production.toml
